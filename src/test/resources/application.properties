# Test Database Configuration
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration for Tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Disable Flyway for tests
spring.flyway.enabled=false

# JWT Configuration for Tests
jwt.secret=test-secret-key-for-testing-only
jwt.expiration=86400000

# Logging for Tests
logging.level.com.healthapp=INFO
logging.level.org.springframework.security=INFO

# OpenAI Configuration for Tests
openai.api.key=test-api-key
openai.timeout=30
openai.model.voice.food-default=gpt-4.1-mini
openai.model.voice.activity=gpt-4.1-mini
openai.model.voice.cycle=gpt-4.1-mini
openai.model.cycle-sync=gpt-4o-mini
nutrition.lookup.enabled=false

# Rate limiting uses a single bucket per client IP; integration tests share one IP and exhaust limits otherwise.
app.rate-limit.enabled=false