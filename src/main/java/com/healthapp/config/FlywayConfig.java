package com.healthapp.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FlywayConfig {
    // Flyway will now run automatically and create the database tables
} 